<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="2dp">

    <ImageView
        android:id="@+id/imgMovieTrend"
        android:layout_width="108dp"
        android:layout_height="192dp"
        android:layout_marginVertical="4dp"
        android:background="@drawable/rounded_background_without_padding"
        android:backgroundTint="@color/orange"
        android:clipToOutline="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/image_placeholder" />

    <TextView
        android:id="@+id/txtVoteTrendMovie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="8dp"
        android:background="@drawable/rounded_background_card"
        android:backgroundTint="@color/orange"
        android:padding="6dp"
        android:text="@string/no_content"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintRight_toRightOf="@id/imgMovieTrend"
        app:layout_constraintTop_toTopOf="@id/imgMovieTrend" />

</androidx.constraintlayout.widget.ConstraintLayout>